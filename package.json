{
    "name": "gawati-keycloak-theme",
    "version": "1.0.0",
    "scripts": {
        "build": "mkdir build && cp -arv account admin common email login welcome LICENSE build/",
        "vars": "env | grep npm_package_ | grep -v npm_lifecycle_script"
  }
}
